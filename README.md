# Manga OCR & Translation Pipeline

Very early WIP.

Goal: build a small, clear pipeline that:
1. Takes a manga page image
2. Runs OCR to extract Japanese text
3. Sends the text to a language model for translation
4. Outputs a numbered list of Japanese + English lines

This repo is being built slowly and step-by-step.